import ModalTitle from "../ModalTitle";
import { DialogContent, DialogActions } from "./dialogStyles";
import Button from "@material-ui/core/Button";
import Dialog from "@material-ui/core/Dialog";
import FormControl from "@material-ui/core/FormControl";
import TextField from "@material-ui/core/TextField";
import Select from "@material-ui/core/Select";
/**
 * @description dialogComponents
 *
 * provides one local import containing components common to the dialogViews.
 *
 * helps ease the transition from one component framework to another.
 *
 * example:
 *    import {TextField, Button } from "./dialogComponents";
 */
export {
  ModalTitle,
  DialogContent,
  DialogActions,
  Button,
  Dialog,
  FormControl,
  TextField,
  Select
};
